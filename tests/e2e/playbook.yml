- name: Deploy Gitlab
  hosts: gitlab
  tasks:
    - name: Import GitLab playbook
      ansible.builtin.import_role:
        name: gitlab
    - name: Import signalilo playbook
      ansible.builtin.import_role:
        name: signalilo
